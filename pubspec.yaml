name: s_liquid_pull_to_refresh
description: A liquid/spring styled pull-to-refresh widget for Flutter with customizable animations.
issue_tracker: https://github.com/SoundSliced/s_liquid_pull_to_refresh/issues
repository: https://github.com/SoundSliced/s_liquid_pull_to_refresh
homepage: https://github.com/SoundSliced/s_liquid_pull_to_refresh
version: 2.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  s_packages: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
